<%= form_with(model: recipe_history) do |form| %>
  <% if recipe_history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_history.errors.count, "error") %> prohibited this recipe_history from being saved:</h2>

      <ul>
        <% recipe_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
